<ol class="postlist">
{{#each collections.posts }}
  <li>
    <a href="{{ this.url }}">
      {{#if this.data.title }}
        {{ this.data.title }}
      {{else }}
        <code>
          {{ this.url }}
        </code>
      {{/if}}
    </a>
  </li>
{{/each}}
</ol>
