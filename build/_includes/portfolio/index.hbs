<ul class="post-list">
  {{#if tag}}
    {{#each (lookup collections tag)}}
      {{> portfolio/projectListItem}}
    {{/each}} 
  {{else}}
    {{#each collection }}
      {{> portfolio/projectListItem}}
    {{/each}}
  {{/if}}
</ul>