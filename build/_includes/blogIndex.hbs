<ol class="postlist">
{{#each Array.from(collections.posts).reverse() }}
  <li>
    <a href="{{ this.url }}">
      {{#if this.data.title }}
        {{ this.data.title }}
      {{else }}
        <code>
          {{ this.url }}
        </code>
      {{/if}}
    </a>
  </li>
{{/each}}
</ol>
